services:
  - type: web
    name: voig-bot
    env: node
    nodeVersion: 20
    plan: free
    buildCommand: npm install
    startCommand: NODE_ENV=production npx tsx script/build.ts && npm run start
    envVars:
      - key: NODE_ENV
        value: production
