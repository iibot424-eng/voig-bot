services:
  - type: web
    name: voig-bot
    env: node
    nodeVersion: 20
    plan: free
    buildCommand: npm install --include=dev && npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
