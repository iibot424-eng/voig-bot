services:
  - type: web
    name: voig-bot
    env: node
    nodeVersion: 20
    plan: free
    buildCommand: npm install && npx tsx script/build.ts
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
