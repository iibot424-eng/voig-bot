services:
  - type: web
    name: voig-bot
    env: node
    nodeVersion: 20
    plan: free
    buildCommand: npm install --production=false && ./node_modules/.bin/tsx script/build.ts
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
